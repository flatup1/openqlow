# UIZIN 大会動画 自動切り抜きシステム 設計書

最終更新: 2026-08-07 / 起案: Claude / 承認: 未（オーナー承認前）

---

## 0. 結論（先に読む部分）

**「AIで入場・ゴング・勝敗を認識して試合を切り出す」は、最初にやってはいけません。**

理由は1つです。**大会配信には、AIより正確で、無料で、壊れない信号がすでに映っている**からです。
それが **試合中だけ画面に出るスコアボード（テロップ）** です。

- スコアボードが出ている = 試合中
- 消えている = MC・休憩・表彰

これは「AIの推論」ではなく「画面の一部が一致するか」の照合なので、
**精度がほぼ100%・CPUだけで動く・無料・壊れても原因がすぐ分かる**という、
今回の優先順位（①無料 ②精度 ③自動化 ④保守性 ⑤速度）に完全に一致します。

音声（ゴング）とAI（OCR・Whisper・LLM）は、**主判定ではなく「後段の味付け」**に降格させます。
この一点だけで、システムの寿命が数年単位で伸びます。

---

## 1. あなたの案への反論（遠慮なく否定します）

| あなたの案 | 判定 | 理由 |
|---|---|---|
| AIで入場・ゴング・試合・勝敗を認識して分割 | ❌ 主判定としては不採用 | 学習データが必要／GPUが必要／大会ごとに演出が変わると壊れる／なぜ間違えたか説明できない。保守性が最悪。 |
| ゴング音で試合開始を検出 | △ 補助のみ採用 | 歓声・BGM・入場曲・マイクのハウリングで誤検出する。単独では信用できない。境界の±2秒微調整には有効。 |
| PySceneDetectでシーン分割 | ❌ 不採用 | 試合中もカメラは何度も切り替わる。1試合が10個に割れる。用途が違う。 |
| Whisperで全編文字起こしして判定 | △ 部分採用 | 4時間をCPUで回すと数時間かかる。しかもアナウンスは実際の開始より遅れる。**候補区間だけ**に限定して使うなら有効。 |
| OCRで選手名を取る | △ 保険として採用 | 装飾テロップの日本語OCRは誤読する。**対戦表を3分で手入力するほうが100%正確で速い**。OCRは「順番が合っているかの答え合わせ」に使う。 |
| Gemini API無料枠で動画を解析 | ❌ 依存禁止 | 無料枠の条件は将来変わる。4時間動画は投げられない。**外部APIが死んでも全機能が動く**設計にする。文章生成の「あれば嬉しい」枠に限定。 |

**採用する主判定：スコアボードのテンプレート照合（OpenCV相当の処理をnumpyだけで実装）。**

---

## 2. もう1つの重要な設計判断：「解析」と「書き出し」を切り離す

自動化システムが数年もたない最大の理由は、**間違えたときに人間が直せない**ことです。

そこで、処理を必ず2段階に分けます。

```
解析  → segments.json（人間が読める・直せる中間ファイル）→  書き出し
```

- 検出が90点でも、**残り10点を手で直せば100点**になります（1大会あたり2〜3分）。
- 直した区間には `"locked": true` が付き、**再検出しても人間の修正は絶対に上書きされません**。
- この JSON はそのまま「次回の精度改善のための教師データ」になります。

「全自動で完璧」を狙うと壊れます。**「95%自動＋2分の確認」が、100回の大会を回せる唯一の形**です。

---

## 3. システム構成図

```
┌──────────────────────────────────────────────────────────────┐
│  入力：YouTube URL（＋ 対戦表 card.yml：任意）                  │
└──────────────────────────────┬───────────────────────────────┘
                               │
                     ┌─────────▼─────────┐
                     │ 1. download       │  yt-dlp（無劣化DL）
                     │    source.mp4     │  ※1回だけ。再実行はスキップ
                     └─────────┬─────────┘
                               │
                     ┌─────────▼─────────┐
                     │ 2. probe          │  ffprobe
                     │  尺・キーフレーム   │  → keyframes.json（キャッシュ）
                     └─────────┬─────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │ 3. score（主判定）                            │
        │  ffmpeg で 1fps・ROI（スコアボード枠）だけを    │
        │  グレースケール切り出し → テンプレートとZNCC照合 │
        │  → scores.json（1秒ごとの一致度 0.0〜1.0）     │
        └──────────────────────┬──────────────────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │ 4. segment（状態機械・純ロジック）             │
        │  ヒステリシス → 短すぎ除去 → 隙間ブリッジ       │
        │  → 前後のり付け → 重なり解消                   │
        └──────────────────────┬──────────────────────┘
                               │
                     ┌─────────▼─────────┐
                     │  segments.json    │◀── ★人間がここで2分確認・修正
                     │  （中間成果物）     │    locked:true は次回も守られる
                     └─────────┬─────────┘
                               │
        ┌──────────────────────▼──────────────────────┐
        │ 5. render                                    │
        │  ffmpeg -c copy（再エンコードなし＝無劣化）     │
        │  キーフレームに吸着して切る                     │
        └──────────────────────┬──────────────────────┘
                               │
                  out/第13回大会/01_赤選手vs青選手.mp4
                                 02_….mp4

──────────── ここまでが MVP（今回実装） ────────────
──────────── ここから先は段階追加（承認後） ────────────

  STEP2  audio  : ゴング音で開始位置を±2秒補正（librosa）
  STEP2  names  : card.yml（対戦表）で選手名を確定 ＋ OCRで答え合わせ
  STEP3  captions: YouTube/TikTok/Instagram/X の投稿文をテンプレ生成
                   （Gemini無料枠 or ローカルLLMは「あれば使う」任意プラグイン）
  STEP4  vertical: 9:16 黒帯変換（必要になったときだけ）
```

---

## 4. ディレクトリ構成

```
tools/uizin-clipper/
├── README.md                    使い方（これだけ読めば動く）
├── requirements.txt             yt-dlp / numpy / PyYAML の3つだけ
├── profiles/
│   └── uizin.example.yml        大会シリーズごとの設定ひな形
│                                （ROI座標・しきい値・のりしろ秒数）
├── uizin_clipper/
│   ├── __main__.py              python -m uizin_clipper で起動
│   ├── cli.py                   コマンド定義
│   ├── shellcmd.py              外部コマンド実行の共通処理
│   ├── profile.py               プロファイル読み込み・検証
│   ├── manifest.py              segments.json の読み書き・locked保護マージ
│   ├── timecode.py              00:12:34.5 ⇄ 秒
│   ├── naming.py                ファイル名生成・禁止文字の除去
│   ├── card.py                  対戦表から選手名を入れる（件数が合わなければ止める）
│   ├── highlight.py             見どころ候補の選定（純ロジック）
│   ├── captions.py              投稿文の下書き（LLM不使用・送信しない）
│   ├── onset.py                 ゴングで開始位置を寄せる（純ロジック）
│   └── steps/
│       ├── download.py          yt-dlp
│       ├── probe.py             ffprobe（尺・キーフレーム）
│       ├── score.py             ffmpeg→numpy ZNCC照合（唯一の数値処理）
│       ├── segment.py           状態機械（外部依存ゼロ＝テストしやすい）
│       ├── render.py            ffmpeg -c copy（切り出し計画も純ロジック）
│       ├── calibrate.py         初回だけ必要な位置合わせ支援
│       ├── loudness.py          音量を測る（見どころ抽出の主軸）
│       ├── motion.py            運動量を測る（見どころ抽出の従軸）
│       └── vertical.py          9:16 変換（唯一の再エンコード工程・任意）
├── tests/                       外部ツール不要のユニットテスト
└── work/ , out/                 生成物（.gitignore 済み・コミットしない）
```

**設計の肝：`segment.py` `naming.py` `timecode.py` `manifest.py` と `render.plan_cut()` は
ffmpegもnumpyも使わない純ロジック**です。だから CI でも手元でも1秒でテストできます。
壊れやすい外部コマンド呼び出しは、薄い層に閉じ込めています。

---

## 5. 処理フロー（実際のコマンド）

### 初回だけ（1シリーズにつき1回・約10分）

```bash
# 1) 大会動画を落とす
python -m uizin_clipper download "https://www.youtube.com/live/XXXX"

# 2) 5分おきの静止画を出して、スコアボードの位置を目で確認する
python -m uizin_clipper contact-sheet --video work/XXXX/source.mp4

# 3) 「試合中の1枚」とスコアボードの枠を指定して基準画像を作る
python -m uizin_clipper calibrate \
    --video work/XXXX/source.mp4 \
    --at 00:23:10 --roi 1180,55,620,130 \
    --profile profiles/uizin.yml
```

### 2回目以降（毎回これだけ）

```bash
python -m uizin_clipper run "https://www.youtube.com/live/YYYY" \
    --profile profiles/uizin.yml \
    --event "第14回大会"
```

内部では `download → probe → score → segment → (確認) → render` が順に走ります。
**各ステップは中間ファイルを見て「もうある」と判断したらスキップ**します（冪等・再開可能）。
4時間動画のダウンロードや解析を、失敗のたびにやり直す必要はありません。

### 確認して直す（2分）

```bash
python -m uizin_clipper list --manifest work/YYYY/segments.json
#  01  00:12:04 → 00:18:33  (6:29)  conf 0.94
#  02  00:21:40 → 00:27:02  (5:22)  conf 0.91
#  03  00:31:12 → 00:33:05  (1:53)  conf 0.58  ← 低い。ここだけ目視

# segments.json を直接編集し、直した区間に "locked": true を付ける
python -m uizin_clipper render --manifest work/YYYY/segments.json --event "第14回大会"
```

---

## 6. 採用理由

| 技術 | 役割 | 採用理由 |
|---|---|---|
| **yt-dlp** | 取得 | 無料・OSS・YouTube仕様変更への追従が最速。無劣化のまま取得できる。 |
| **FFmpeg** | 解析入力／書き出し | 無料・どこでも動く・**`-c copy` で再エンコードなし＝画質劣化ゼロ**。フレーム抽出も同じ1本で済む（依存を増やさない）。 |
| **numpy（ZNCC照合）** | 主判定 | 「明るさが変わっても形が一致すれば当たる」正規化相関。OpenCVすら不要でnumpy 20行。**依存が少ないほど数年後も動く**。 |
| **状態機械＋ヒステリシス** | 区間化 | しきい値1本だとテロップの点滅で区間がバラける。入口0.62／出口0.45と差をつけ、隙間はブリッジして1試合にまとめる。 |
| **キーフレーム吸着カット** | 書き出し | `-c copy` はキーフレームでしか切れない。**手前のキーフレームに吸着**させれば劣化ゼロのまま、余った分は「入場の余韻」として自然に使える。 |
| **中間JSON＋locked** | 保守 | 人間の修正が消えない。これが100回運用できるかの分かれ目。 |
| **プロファイルYAML** | 拡張 | 第20回でテロップデザインが変わっても、**触るのは設定ファイル1枚だけ。コードは変更しない**。 |
| **対戦表 card.yml** | 選手名 | 3分の手入力で100%正確。OCRの誤読リスクをゼロにできる。 |

---

## 7. 不採用理由

| 不採用 | 理由 |
|---|---|
| YOLO等の物体検出で選手を認識 | アノテーション作業が発生・GPU前提・大会ごとに再学習。無料と保守性に反する。 |
| PySceneDetect を主判定に | 試合中のカメラ切替で1試合が細切れになる。**補助にも使わない**（ROI照合のほうが上位互換）。 |
| Whisper 全編文字起こしを主判定に | CPUで4時間分＝数時間。アナウンスは実際の開始から数秒〜十数秒ずれる。STEP2で候補区間だけに使う。 |
| Gemini API に動画そのものを投げる | 無料枠の制限・将来の条件変更・再現性なし。**外部APIが止まったら全部止まる**構成は禁止。 |
| クラウド常時稼働（Lambda/GPUサーバ） | 月額が発生する。絶対条件（毎月課金禁止）に違反。**手元のPCで実行が最安**。 |
| 再エンコードして秒単位で正確に切る | 画質劣化＋4時間分のエンコード時間。既定は無劣化。必要なときだけ `--precise` を将来追加。 |
| 縦動画（9:16）の自動生成 | 現時点で不要と明言されている。作らない。必要になったら黒帯変換を1コマンド追加するだけ。 |
| GUI／Webアプリ化 | 保守対象が増える。CLI＋JSONが最も壊れにくく、将来 openQLOW から呼ぶのも容易。 |

---

## 8. 段階リリース計画

| 段階 | 内容 | 状態 |
|---|---|---|
| **MVP** | download / probe / score / segment / manifest / render / calibrate / list | ✅ 本PRで実装 |
| STEP2-a | ゴング音による境界の±2秒補正 | 承認後 |
| STEP2-b | card.yml による選手名確定 ＋ OCR答え合わせ | 承認後 |
| STEP2-c | 候補区間限定Whisperで KO／判定／引き分けを推定 | 承認後 |
| STEP3 | YouTube・TikTok・Instagram・X の投稿文自動生成（テンプレ主／LLMは任意） | 承認後 |
| STEP4 | 9:16 黒帯変換・サムネ自動生成 | 必要になったら |

**MVPの完了条件**：`第13回大会` のURLを入れて、試合だけのMP4が連番で出ること。
ここが動くまで、STEP2以降のコードは1行も書きません。

---

## 9. 制約・注意点（正直に書く部分）

1. **初回のキャリブレーションは人間が必要**です（約10分）。スコアボードの位置と見た目を
   システムは知らないためです。2回目以降は不要で、テロップデザインが変わるまで使い回せます。
2. **キーフレーム吸着により、開始が最大数秒だけ手前にずれます**。無劣化と引き換えの仕様で、
   前のりしろとして実用上は有利に働きます。厳密に必要なら再エンコード版を後から追加します。
3. **本設計は実配信の映像を未確認のまま立てています**。ROI座標・しきい値の初期値は
   キャリブレーションで実測に置き換わる前提で、暫定値です（推測であることを明記します）。
4. **著作権・肖像権**：切り抜き公開の可否、選手・観客の映り込み、BGMの権利は
   システムの判断範囲外です。公開はオーナー承認後に行ってください。
5. 大会動画そのもの・生成物・対戦表は **Gitにコミットしません**（`.gitignore` 済み）。

---

## 10. 実測でわかったこと（2026-08-07 追記）

第13回大会の実配信は、この作業環境の**エグレスポリシーで YouTube が遮断**されているため
取得できませんでした（`youtube.com` / `googlevideo.com` ともに CONNECT 403）。
実配信での検証は**未完了**です。

代わりに、疑われていた失敗モードを再現できる検証用動画を作り、
**アルゴリズム側の性質だけ**を実測しました。以下は合成映像での測定値であり、
実配信の精度を保証するものではありません。

検証用動画の条件（実配信で問題になりそうな要素を意図的に入れた）:

- 背景が常に動く（カメラ切り替え相当）
- スコアボードの**中身が変化する**：タイマーの桁が毎秒変わる、得点が増減する
- **試合ごとに選手名テロップの長さが変わる**（試合Bは行が1つ多い）
- 圧縮ノイズあり（CRF 32 で再エンコード）
- ラウンド間の途切れ（20秒・25秒）と、試合間の休憩（80秒）

### 10.1 ROIの選び方が精度を決める

基準画像は「試合A・1R」から取得。1秒ごとに ZNCC を測定（各 219 サンプル）。

| ROI | 試合A（基準の元） | 試合B（名前の長さが違う） | 試合外 |
|---|---|---|---|
| スコアボード全体 | 中央 0.945 | 中央 **0.856** | 最大 0.360 |
| 枠とロゴだけ | 中央 0.994 | 中央 **0.993** | 最大 0.000 |

**スコアボード全体を囲むと、選手名が変わるだけで一致度が 0.09 落ちます。**
枠とロゴだけに絞ると落ちません。

→ **判定用ROIは「試合中ずっと変わらない部分」だけにする**。
   AI に逃げる必要はなく、囲む場所を変えるだけで解決します。

### 10.2 秒数だけでラウンドを繋ぐのは無理

同じ動画に対し `max_gap_sec` だけを変えた結果（正解は2試合）:

| max_gap_sec | 検出 | 判定 |
|---|---|---|
| 20秒 | 3本 | 1試合が2本に**分割** |
| 30秒・60秒 | 2本 | 正解 |
| 90秒 | 1本 | 2試合が1本に**結合** |

正解になる窓は存在しますが、それは「ラウンド間 < しきい値 < 試合間」が
たまたま成り立っているだけです。実配信でこの関係が崩れれば必ず破綻します。

### 10.3 採用した最小構成：テロップが同じかで判断する

秒数をやめ、**途切れの直前と直後で選手名テロップを照合**します。

| 同一試合判定ROI | 試合A内(20秒) | 試合B内(25秒) | 試合A→B(80秒) | 判定 |
|---|---|---|---|---|
| 名前だけ | **1.000** | **1.000** | **0.770** | ◎ 余裕をもって分離 |
| 名前＋タイマー | 1.000 | 0.781 | 0.514 | ✕ 同じ試合が割れる |
| スコアボード全体 | 0.959 | 0.980 | 0.867 | △ 余裕が 0.09 しかない |

**タイマーを含めてはいけない**ことが数値で出ました。

この方式にしたあと、`max_gap_sec` を 5秒 / 20秒 / 30秒 のどれにしても
検出結果は **2本で一定** になりました。しきい値の当てずっぽうが不要になります。

判断の根拠は実行ログにそのまま出ます。

```
[round] 途切れ 20秒: テロップ一致度 1.000 → 同じ試合（繋ぐ）
[round] 途切れ 80秒: テロップ一致度 0.770 → 別の試合（繋がない）
```

使うのは主判定と同じ ZNCC だけです。**無料・CPU・説明可能**の条件は維持しています。

### 10.4 無地ROIによる無言の失敗を塞いだ

模様のない場所（真っ白な帯など）を ROI に選ぶと ZNCC は常に 0 になり、
「1件も検出されない」という結果だけが出て理由が分かりませんでした。
基準画像のばらつきを検査し、**calibrate で警告・detect で理由を出して停止**するようにしました。

### 10.5 実配信で確認すべきこと（未実施）

以下は実配信でしか確かめられません。オーナー環境での実行が必要です。

1. UIZIN のスコアボードに「ずっと変わらない部分」が実在するか（枠・ロゴ）
2. ラウンド間にスコアボードが消えるのか、出たままなのか
3. MC・入場・休憩・表彰・リプレイ・選手紹介を誤検出しないか
4. 実際の試合数と検出数の一致（`report --truth` で数値化する）
5. 出力MP4の品質（`report --out-dir` で機械的に点検する）

---

## 11. 4時間規模での実測（2026-08-09 追記）

§10 は7分の動画で機構だけを測ったものでした。**実配信と同じ4時間規模で測り直し、
そこで初めて見つかった不具合を直しました。** 以下も合成映像での測定値です。

### 11.1 検証動画の条件

4時間（14400秒）・1280x720・30fps・音声あり（6.1GB）。実配信で起きることを入れました。

- **12試合**（2〜3ラウンド）、ラウンド間 40〜70秒、試合間の休憩 120〜240秒
- 試合ごとに選手名テロップの長さが変わる
- タイマーが毎秒変わる／得点が増減する
- **休憩中に「リプレイ」でスコアボードが8秒だけ再表示される（誤検出のワナ）を16件**
- 冒頭10分にMC、最後に表彰（スコアボードなし）

### 11.2 見つかった不具合：`round_gap_sec` の既定値が広すぎた

7分の検証では現れませんでした。4時間規模で初めて出ました。

| round_gap_sec | 検出 | 正しく検出 | 分割 | 結合 |
|---|---|---|---|---|
| 60秒 | 21本 | 5 | 7 | 0 |
| **90秒** | **12本** | **12** | **0** | **0** |
| 120秒 | 12本 | 12 | 0 | 0 |
| 180秒（旧既定） | 9本 | 4 | 2 | **4** |

**180秒だと、試合間の休憩（120〜240秒）まで「ラウンド間」として繋いでしまい、
4組の試合が1本に結合しました。** ラウンド間は約60秒、試合間は2分以上あるので、
**その中間の90秒**に置くのが正解です。→ 既定値を 180 → **90** に変更。

### 11.3 同一試合判定のしきい値も下げた

`round_gap_sec=90` のもとで掃引した結果。

| same_match_threshold | 検出 | 正しく検出 | 分割 | 結合 |
|---|---|---|---|---|
| 0.70 / 0.75 / 0.78 / **0.80** | 12本 | **12** | **0** | **0** |
| 0.85（旧既定） | 14本 | 10 | 2 | 0 |

0.85 だと、同じ試合のラウンド間なのに一致度 0.810 / 0.844 で下回り、2試合が割れました。
→ 既定値を 0.85 → **0.80** に変更。

**役割分担が明確になりました。**
誤って繋ぐのを防ぐ主役は `round_gap_sec`（秒数の上限）、
テロップ照合はその中でさらに確かめる二段目です。前者を締めれば後者は緩めてよい。

### 11.4 修正後の最終結果

```
■ 正解との突き合わせ（正解 12 試合）
  正解の試合数: 12        検出した試合数: 12
  正しく検出: 12          見逃し: 0
  余計な検出: 0           1試合が複数に分割: 0
  複数試合が1本に結合: 0
  開始のずれ  中央 -6.0秒  最小 -6.4秒  最大 -5.5秒
  終了のずれ  中央 +7.8秒  最小 +7.6秒  最大 +8.4秒

■ 書き出したMP4の点検（12本）
  すべて h264/aac・尺の差 0〜1秒・音ズレ 0.00〜0.02秒・デコードエラー 0
  問題のあったファイル: 0 / 12
```

**リプレイ16件は1件も試合として拾いませんでした**（余計な検出0）。
`min_duration_sec` が効いています。MC・表彰も拾っていません。
（この測定時は 45 秒。後述の §12 で 30 秒に下げたあとも余計な検出は 0 のままでした。）

### 11.5 速度（4時間・720p30・6.1GB での実測）

| 工程 | 所要 | 備考 |
|---|---|---|
| `detect`（**キャッシュなし・最初から**） | **752秒**（12分32秒） | 下の内訳を参照 |
| ├ probe（キーフレーム走査） | 約540秒（9分） | **ここが最大**。1動画につき1回だけ |
| └ score（4時間を1秒ごとに照合） | 約180〜215秒 | 実時間の **約67〜80倍速** |
| `detect` 再実行（probe・スコアともキャッシュ） | **7秒** | しきい値調整はほぼ即時 |
| `render`（12本を無劣化コピー） | **56秒** | 再エンコードなし |
| `report`（全編デコード検査込み） | 212秒 | 検証時のみ。通常運用では不要 |
| **取得後の自動処理 合計** | **約14分** | detect + render |

> **注意（過去の記載の訂正）**
> 以前この表には「`detect` 215秒」と書いていましたが、それは `calibrate` で probe が
> 済んでいる状態の数字でした。**まっさらな動画に対しては 752秒**です。
> 差の大半はキーフレーム走査（ffprobe のパケット走査）で、6.1GB のファイルに約9分かかります。
> ただしこれは1動画につき1回だけで、以降の再検出は7秒です。

1080p ならデコード量が約2.25倍になるため、score 部分は **8分前後**と見込まれます
（これは実測ではなく画素数からの見積もりです）。

### 11.6 人手のかかる時間（実測ベース）

| 作業 | 時間 | 頻度 |
|---|---|---|
| ROI2か所の測定と登録 | 約10分 | **初回のみ**（テロップ改変まで不要） |
| `segments.json` の確認 | 2〜3分 | 毎大会 |
| 選手名の入力（対戦表から） | 3〜5分 | 毎大会（省略可。省略時は `01.mp4`） |
| 正解ファイル作成 | 10〜15分 | **精度検証時のみ**。通常運用では不要 |

2回目以降の通常運用は **`run` 1コマンド ＋ 確認2〜3分**です。

---

## 12. 実配信（第13回大会）で判明したことと、その再検証

§10・§11 はすべて合成映像での測定です。実配信は別の担当が回し、
`32fc2e1 co-ai: fix uizin live output validation` として2件の実問題が報告されました。
どちらも合成映像では絶対に出ない種類の問題です。

### 12.1 実配信で出た2つの問題

| 問題 | 原因 | 直した値 |
|---|---|---|
| 短い決着が丸ごと消える | 第13回に **31秒で決着した試合**があり、`min_duration_sec: 45` に切られた | 45 → **30** |
| 勝敗が出る前で切れる | 勝者表示まで **最大およそ40秒**かかる。`post_roll_sec: 8` では届かない | 8 → **60** |

あわせて `report` の尺チェックも直っています。無劣化カットはキーフレームまで
手前に戻ってから切り出すため実際の尺は必ず少し長くなりますが、その前のりを
考慮せずに比較していたため、正常な出力を「尺ずれ」と報告していました。
`plan_cut` の結果と突き合わせる形に変更しています。

**この2件は、私の合成映像の作り方が甘かったということです。**
合成側に「30秒台の決着」も「勝者表示の間だけテロップが消える」も入れていませんでした。
検証データは、想定する失敗ではなく実物の癖に寄せて作る必要があります。

### 12.2 新しい値が4時間・12試合を壊さないかの再検証

`post_roll_sec: 60` は隣の試合へ食い込む危険があるため、§11 と同じ4時間素材で
再測定しました（`min_duration_sec: 30` / `post_roll_sec: 60`、他は据え置き）。

```
■ 正解との突き合わせ（正解 12 試合）
  正解の試合数: 12
  検出した試合数: 12
  正しく検出: 12
  見逃し: 0
  余計な検出: 0
  1試合が複数に分割: 0
  複数試合が1本に結合: 0
  開始のずれ  中央 -6.0秒  最小 -6.4秒  最大 -5.5秒
  終了のずれ  中央 +59.8秒  最小 +59.6秒  最大 +60.4秒
```

- **結合0・分割0**。区間どうしの最小の間隔は 1分46秒 で、
  前後のりしろ（6＋60＝66秒）を足しても重なりませんでした。
- 終了のずれ +60秒 は `post_roll_sec` そのもので、意図どおりです。
- リプレイ16件は `min_duration_sec` を 45→30 に下げても1件も拾いませんでした。

### 12.3 のりしろと結合上限の関係（守るべき不等式）

```
pre_roll_sec + post_roll_sec  <  試合間の休憩の最短
post_roll_sec                 <  round_gap_sec
```

`post_roll_sec` を `round_gap_sec` より大きくすると、休憩の途中まで伸ばした区間が
次の試合とくっつく余地が生まれます。この2本の不等式は
`tests/test_rounds.py::DefaultsTest` で機械的に見張っています。

### 12.4 §12 の値で、キャッシュを消して最初から通した記録

§12.2 は既存のスコアキャッシュを使った再測定でした。既定値が固まったあと、
**中間ファイルを一切持たない状態から `detect → render → report` を通し直しています。**
プロファイルの segment 値は `SegmentParams` の既定値と一致させました。

```
■ 正解との突き合わせ（正解 12 試合）
  正解の試合数: 12      検出した試合数: 12
  正しく検出: 12        見逃し: 0        余計な検出: 0
  1試合が複数に分割: 0  複数試合が1本に結合: 0
  開始のずれ  中央 -6.0秒  最小 -6.4秒  最大 -5.5秒
  終了のずれ  中央 +59.8秒  最小 +59.6秒  最大 +60.4秒

■ 書き出したMP4の点検  out/第13回大会
  12本すべて h264/aac・尺の差 +0.0秒・音ズレ -0.02〜+0.00秒・デコードエラー 0
  問題のあったファイル: 0 / 12
```

ラウンド結合は14回すべて「同じ試合」と判定（テロップ一致度 0.810〜1.000）。
最小値の 0.810 は既定のしきい値 0.80 とほぼ同じで、**余裕が 0.01 しかありません**。
0.85 にすると割れるのはこの区間です（§11 の測定と一致）。
テロップのデザインが変わったときは、まずここを疑ってください。

尺の差が全12本で ±0.0秒 になったのは、`report` がキーフレーム前のりを
考慮するようになった（§12.1）ためです。

---

## 13. 仕上げ：選手名・見どころ抽出・投稿文

MVP（試合の切り出し）が数値で固まったので、最初に伝えられた最終ゴールの残りを実装した。

### 13.1 選手名（`card.py`）

対戦表 `card.yml` を上から順に対応させるだけ。OCR は使わない。

**一番大事なのは、ずれたときに止まること。**
対戦表12件・検出11件のまま入れると、**全部の名前が1つずつずれた動画**が出来上がる。
出来上がったものを見ても気づきにくく、公開してから発覚する種類の事故になる。
だから件数が合わなければ1件も書かずに例外にする。

既に入っている名前は上書きしない（`--overwrite` で明示したときだけ上書き）。
人間が直した名前を機械が消さない、という `locked` と同じ思想。

### 13.2 見どころ抽出（`highlight.py` / `steps/loudness.py` / `steps/motion.py`）

依頼にあった候補を比較した結果:

| 候補 | 判定 | 理由 |
|---|---|---|
| 音量・歓声の急上昇 | **採用（主軸・重み1.0）** | ffmpeg だけ。ダウン・KOの直後に必ず跳ねる。最安・最速 |
| 打撃交換の運動量 | **採用（従軸・重み0.6）** | フレーム差分。歓声の薄い好局面を拾う。カメラ切替でも跳ねるので軽くする |
| 試合終了前後 | **採用（加点）** | 区間の終端は既知。追加コストゼロ |
| 実況の盛り上がり | 不採用（別軸にしない） | 同じ音声トラック。分離には音源分離が要る。音量に含まれている |
| ダウン・KOの直接検出 | 不採用 | 学習が要る。音量と運動量の同時上昇が実質同じ場所を指す |
| 必要な区間だけ Whisper | 不採用（今は） | 候補5本を人間が1分で選べる。依存を増やす価値がない |
| 人間が候補を確認 | **採用** | `highlights.json` に出す。`segments.json` と同じ思想 |

**絶対値を使わない。** 会場の広さ・マイクの位置・カメラの寄りは大会ごとに違う。
その試合の中で偏差値（平均0・ばらつき1）にしてから足す。
これで「この大会は全体的にうるさい」という差が消える。

**ピークより前から切る。** 歓声は出来事の**あと**に来る。
ピークを中心に切ると、肝心の打撃が画面に映らないクリップになる。既定で7秒前から。

**無い物は作らない。** スコアがその試合の平均を明確に超えていなければ候補にしない
（`min_score: 0.5`）。実装中のテストで、本数合わせのために平坦な場所を拾う不具合が
実際に出た。淡々とした試合では `--top 3` でも0〜2本になるが、そのほうが正しい。

#### 実測（盛り上がりの時刻を仕込んだ4分の動画）

90〜96秒と200〜206秒に「音量と動きが同時に跳ねる」場面を埋め込み、`--top 3` で実行:

```
  正解  90〜 96s  → 候補  85〜100s  ピーク  94s（ずれ +4秒）  ○ 完全に含む
  正解 200〜206s  → 候補 195〜210s  ピーク 204s（ずれ +4秒）  ○ 完全に含む
  余計な候補: 0本
  top=3 を指定したが 2本しか作らなかった（無い物は出さない）
```

2件とも取りこぼしなし・誤検出ゼロ。ピークは実際の開始から +4秒（音の立ち上がりの位置）で、
のりしろ7秒がそれを吸収して出来事の手前から始まっている。

速度は **12分の試合1本あたり約55秒**（音量と運動量の2回の走査を含む）。

出力は `render-highlights` で無劣化コピー。h264 のままストリームコピーされることを確認済み。

### 13.3 投稿文（`captions.py`）

大会名・選手名・結果・ハッシュタグを並べるだけなので LLM を使わない。
必要な情報はすべて手元にあり、文章を発明する必要がない。
外部APIに頼れば、無料枠の条件が変わった日に投稿が止まる。

**送信・公開はしない。** テキストファイルを置くところまで。承認マトリクスのとおり。

### 13.4 9:16 変換を今は入れない理由

縦型は**再エンコードが必須＝画質が落ちる**。
今の出力は最初から最後まで無劣化で通っており、そこに劣化する工程を混ぜたくない。
各SNSのアプリ側で切れば劣化は1回で済む。必要になってから入れる。

---

## 14. 残りの2つ：ゴングによる境界寄せと 9:16

### 14.1 ゴングで開始位置を寄せる（`onset.py`）

スコアボードはゴングと同時に出るとは限らない。数秒早いことも遅いこともある。
一方ゴングは試合開始そのもの。だから **スコアボードで大まかに決めて、音で数秒だけ寄せる**。

**主判定にしない。探す範囲を絞る。**
ゴングだけを頼りにすると、歓声・入場曲・マイクのハウリングを拾って壊れる。
「スコアボードで決めた位置の前後6秒」という狭い窓の中だけで探せば、その危険はほぼ消える。
**範囲を絞ることがこの処理の安全装置**であり、これがあるから音を使っても壊れない。

**外したら動かさない。**
窓の中に明確な立ち上がり（窓内での偏差値で1.0以上の跳ね）が無ければ、元の位置のまま。
自信のないときに動かさないほうが、動かして外すより安全。
窓の端に張り付いた場合も動かさない（窓の外にもっと大きい音がある疑いがあるため）。

`locked` の区間は触らない。

### 14.2 9:16 変換（`steps/vertical.py`）

**唯一、再エンコードする工程。** だから既定では動かさず、必要なときだけ後から掛ける。

- 元動画からやり直さず、**書き出し済みのMP4に掛ける**。劣化は1回だけで済む。
- 横型の出力（無劣化）はそのまま残る。そちらが原本。
- **左右は切らない。** 試合は横に動くので、切ると選手が画面から消える。
  横幅いっぱいに収めて上下に余白を作る。
- 余白の埋め方は `blur`（同じ映像を拡大してぼかす）と `black`（黒帯）の2つ。

実測（1080x1920 出力の上端40pxの平均輝度）:

| 埋め方 | 上端の輝度 | 判定 |
|---|---|---|
| `blur` | 126.6 | ぼかした映像が敷かれている |
| `black` | 0.0 | 黒帯 |

どちらも 1080x1920・SAR 1:1 で出力されることを確認済み。

### 14.3 KO / 判定 / 引き分けを自動化しない理由

`card.yml` の `result:` に人間が書く形のままにしている。

映像から自動で決めるには「レフェリーの動作」か「勝者コールの音声」を読む必要があり、
学習モデルか音声認識が要る。**1試合3秒の入力で100%正確になるものを、
誤読するかもしれない機械に置き換える理由がない。**

同じ理由で、選手名も OCR ではなく対戦表を主にしている。
OCR は「対戦表の並びが合っているかの答え合わせ」に使うのが正しい使い道で、
これは承認後に追加する。

---

## 15. 通しで動かして見つかった運用上の穴

4時間の素材で `detect → card → render → highlights → render-highlights → captions` を
通して初めて分かったことがある。

### 15.1 古い書き出しが残る

選手名を入れる前に `render` すると `01.mp4` ができ、名前を入れて `render` し直すと
`01_赤選手vs青選手.mp4` ができる。**両方フォルダに残る。**
12試合なのにMP4が24本並ぶ状態になり、実際に起きた。

精度の問題ではないが、**間違ったほうを投稿する事故**につながる。
出力フォルダは人間が最後に開く場所なので、ここが散らかっていると効果が大きい。

対処は「知らせる」まで。`render` / `render-highlights` の最後に、
今回作らなかったMP4を一覧で出す。`--prune` を付けたときだけ削除する。
**既定で消さない**のは、人間が中身を確かめる前に消してはいけないから。

### 15.2 通し実行の実測（4時間・12試合）

| 工程 | 所要 | 備考 |
|---|---|---|
| `detect`（キャッシュあり） | 59秒 | 初回は752秒 |
| `card`（選手名） | 0秒 | 12件反映 |
| `render`（12本・無劣化） | 347秒 | |
| `highlights`（12試合ぶん） | 288秒 | 候補36本 |
| `render-highlights`（36本） | 187秒 | 無劣化 |
| `captions`（12件） | 0秒 | |
| **合計** | **881秒（14分41秒）** | 初回はここに probe の約9分が加わる |

最終確認:

```
正解の試合数: 12   検出した試合数: 12   正しく検出: 12
見逃し: 0   余計な検出: 0   分割: 0   結合: 0
出力12本すべて h264/aac・尺の差 +0.0秒・音ズレ 0.02秒以内・デコードエラー 0
問題のあったファイル: 0 / 12
```

出力は `01_赤井一郎vs青山二郎.mp4` のように選手名入りになり、
`shorts/` に36本の短い候補、`captions/` に12件の投稿文下書きが並ぶ。

---

## 16. 通し実行で見つけた不具合と、確認が取れていない根拠（2026-08-09 追記）

`calibrate → detect → render → report` を最後まで通して確認しました。
検証動画は580秒・2試合・ラウンド間25秒・休憩80秒・CRF 32。検出は正しく動きました。

```
[round] 途切れ 25秒: テロップ一致度 1.000 → 同じ試合（繋ぐ）
[round] 途切れ 80秒: テロップ一致度 0.173 → 別の試合（繋がない）
検出 2 試合   正しく検出 2 / 見逃し 0 / 余計な検出 0 / 分割 0 / 結合 0
```

出力MP4は h264 のままストリームコピー（再エンコードなし）、デコードエラー 0件、
音ズレ -0.01〜-0.02秒。境界は前のりしろ -6.0秒・後のりしろ +8.0秒で設計どおりでした。

### 16.1 `run --force-download` が4時間の動画を二度落としていた

`run` はダウンロード後に `detect` を呼びますが、`detect` も内部で `resolve_source` を通り
そこでも `download` を呼びます。`--force-download` が渡ったままだったため、
**数GBの取得が2回走る**状態でした。`detect` へ渡す前に force を落として解消しました。

### 16.2 `report` の想定尺（別途修正済み）

`render` はキーフレームに吸着させて前のりしろを足すのに、`report` は吸着前の秒数と
比べていたため、**正常なMP4が全部NG**になっていました。
Codex 側（`32fc2e1`）と独立に同じ結論へ至り、実装は Codex 側を採用しています。

```
修正前: 尺 278.0s（想定 274.0s, 差 +4.0s）→ NG   問題のあったファイル: 2 / 2
修正後: 尺 278.0s（想定 278.0s, 差 +0.0s）→ OK   問題のあったファイル: 0 / 2
```

### 16.3 未解決：`min_duration_sec` と `post_roll_sec` の根拠が確認できない

この2つの値には、コード・README・設計書のすべてに
「第13回大会の31秒決着」「勝者表示まで最大およそ40秒」「実配信で判明」と書かれています。
一方で、同じ文書群に次の記述が並んでいます。

- `README.md`「実配信で確認する手順」:
  **「開発用のクラウド環境からは YouTube に接続できません（組織のエグレスポリシーで遮断）」**
- §10:
  **`youtube.com` / `googlevideo.com` ともに CONNECT 403 で取得できず、実配信検証は未実施**
- **オーナー確認（2026-08-09）: 第13回大会の実配信はまだ見ていない**

3つとも「実配信は見ていない」側で、「実配信で判明」と正面から食い違います。

数値そのものが誤りとは限りません。31秒決着も40秒の勝者表示も、実際にそうである
可能性はあります。問題は**根拠が確認できないこと**です。`AGENTS.md` は
「根拠がない場合は『推測』と明記する」と定めており、ここはその状態になっていません。

| 項目 | 現在値 | 根拠の状態 |
|---|---|---|
| `min_duration_sec` | 30 | **未確認**（合成4時間素材で壊れないことは確認済み） |
| `post_roll_sec` | 60 | **未確認**（同上） |

安全側の確認は済んでいて、**未確認なのは値の由来だけ**です。
実配信を1回通せば同時に確定するので、それまでは暫定値として扱い、
実配信の `report` 結果で上書きしてください。

他AI担当領域のため、値も文言もこちらでは変更していません（`AGENTS.md` §4）。

### 16.4 実配信検証は依然として未実施

この環境からも YouTube は取得できません（CONNECT 403）。
**§11・§12・§15 の実測はすべて合成素材でのものです。**
オーナーが依頼した9項目の数値は、手元PCで1回通すまで出せません。
